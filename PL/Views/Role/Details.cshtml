@model RoleViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Role Details</h1>

<div>

    <partial name="PartialViews/Details-Delete-Partialview" />

    <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-primary">Update</a>
    <a asp-action="Index" class="btn btn-light">Back To List</a>
</div>
