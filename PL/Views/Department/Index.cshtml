@model IEnumerable<Department>

@{
    ViewData["Title"] = "Index";
}
<h1>All Departments</h1>

<br />

<a asp-action ="Create" class="btn btn-primary">Create New Department</a>      @*  button (create new department) *@

<br />


@if (Model.Count() > 0)

{
    <table class="mt-5 table table-striped table-hover">
        <thead>
            <tr>
                <td>@Html.DisplayNameFor(D => D.code)</td>
                <td>@Html.DisplayNameFor(D => D.Name)</td>
                <td>@Html.DisplayNameFor(D => D.CreatedAt)</td>
                <td>Details</td>                                          @* buttons *@
                <td>Update</td>
                <td>Delete</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var department in Model)                           //loop of (departments) // model (refer to) = IEnumrable of <dep>
            {
                <tr>
                    <td>@department.code</td>
                    <td>@department.Name</td>
                    <td>@department.CreatedAt</td>

                    <td>
                        <a asp-action="Details" asp-route-id ="@department.Id" class="btn btn-warning">
                            <i class="fas fa-eye"></i> Details              @* take id *@ @*  شايله ال id بتاع ال dep *@ 
                        </a>
                    </td>

                    <td>
                        <a asp-action="Update" asp-route-id="@department.Id" class="btn btn-success">
                            <i class="fas fa-eye"></i> Update
                        </a>
                    </td>

                    <td>
                        <a asp-action="Delete" asp-route-id="@department.Id" class="btn btn-danger">
                            <i class="fas fa-eye"></i> Delete
                        </a>
                    </td>


                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="mt-5 alert alert-warning">
        <h3>there is No Departments </h3>
    </div>
}
